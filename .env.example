# Trivia Advisor V2 - Environment Variables

# Database Connection
# Use SUPABASE_DATABASE_URL for Supabase PostgreSQL
# Or DATABASE_URL for standard PostgreSQL connection
SUPABASE_DATABASE_URL=postgresql://user:password@host:5432/database
# DATABASE_URL="postgresql://user:password@localhost:5432/trivia_advisor_prod"

# Production Environment Variables
# (Not needed in development)

# Secret key base for signing cookies and sessions
# Generate with: mix phx.gen.secret
# SECRET_KEY_BASE=your_secret_key_here

# Phoenix server host (production domain)
# PHX_HOST=quizadvisor.com

# Base URL for sitemap and SEO (defaults to https://PHX_HOST)
# BASE_URL=https://quizadvisor.com

# HTTP port (default: 4003)
# PORT=4003

# Database pool size (default: 10)
# POOL_SIZE=10

# Enable IPv6 for Ecto (default: false)
# ECTO_IPV6=false

# DNS cluster query for multi-node deployments (optional)
# DNS_CLUSTER_QUERY=

# Enable Phoenix server (for releases)
# PHX_SERVER=true
